namespace BonusSystemApplication.Models.ViewModels
{
    public class HomeFormViewModel //Controller name | Action name | ViewModel
    {
        //must: combine names: LName + FName
        //may : split domain models into several to represent different parts of form:
        //      objectives, results, comments, objective signatures, results signatues


    }
}
